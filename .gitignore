# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit real secrets
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local tooling
.claude/

# raw pre-conversion source media (HEIC/MOV originals, reference shots) —
# huge and never meant to ship; public/products/all/ has the converted,
# web-ready versions that actually get served.
/public/products/_heic_originals/
/public/products/_mov_originals/
/public/products/_reference/

# Supabase CLI local cache
/supabase/.temp/

# stray local artifacts — never commit session/auth tokens
admin_cookie.tmp.txt
