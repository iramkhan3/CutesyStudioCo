/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Real product photos will likely be hosted on Supabase Storage or a CDN.
    // TODO: add your real image host(s) here once you're serving real product photos, e.g.:
    // remotePatterns: [{ protocol: 'https', hostname: '*.supabase.co' }],
  },
};

export default nextConfig;
